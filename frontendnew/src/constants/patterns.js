export const EMAIL = {
    value: /^[\w-.]+@([\w-]+\.)+[\w-]{2,63}$/i,
    message: 'Email Is Not Valid',
  };
export const PHONE_NUMBER = {
  value: /(6|7|8|9)\d{9}/,
  message: 'Phone Number not Valid',
}